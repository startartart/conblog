import { ReactNode } from 'react';

export type Props = {
  $fontSize?: string;
  $color?: string;
  $hoverColor?: string;
  $isInActive?: boolean;
  $lineHeight?: number;
  $margin?: string;
  children?: ReactNode;
};
